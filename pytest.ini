[pytest]
testpaths = tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    network: needs the live internet (NATIVE_PLAN.md N5b's HTTPS acceptance gate);
        skipped automatically under FUNNY_NO_NET=1, deselect with '-m "not network"'
