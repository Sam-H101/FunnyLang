squad Animal {
    spawn(name) { me.name = name }
    bet speak() { yap me.name, "makes a noise" }
}
squad Dog inherits Animal {
    spawn(name) { og.spawn(name) }
    bet speak() {
        og.speak()
        yap me.name, "barks"
    }
}
Dog("fido").speak()
