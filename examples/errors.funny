bet divide(a, b) {
    bounce a / b
}
sketchy {
    yap divide(10, 2)
    yap divide(10, 0)
} my_bad (e) {
    yap "caught:", e.flavor
    yap "message:", e.message
} regardless {
    yap "cleanup always runs"
}
yap "still running"
bet main() {
    yo name = "sam"
    yap name + 5
}
main()
